## New feature

- Create a new collection.
  - Name: Games.
  - Schema: Title, system (PC, Nintendo NES, Sega Genesis, etc.).
  - Studio: the name of the studio that developed the game.
- Integrate IGDB API and adjust according to the type of data the API returns the Schema of the collection.
  - If theres no data that matches the schema, please tell me to see if we can omit the value.
  - If there is authentication data that you need from the API just tell me.
- Create all the necessary content on the web. Just like what all the other collections have.
  - A row on the home page.
  - A dedicated page to see all the games.
  - A slug page for the individual items.
  - Add the category on the profile section just like all the other in order to filter what the user has reviewed.
  - If im missing some other page or feature please tell me.
- Don't commit any code.
- Don't push any code to github. Keep it local.
- Don't use 'any' for typescript types, if you have to create an interface or a type, just created, is preferred over using 'any'.
